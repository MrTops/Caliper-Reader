import { useEffect, useRef, useState } from "react";
import { Motion, spring } from "react-motion";

const Caliper = props => {
    return (<Motion style={{value: spring(chase),}}>
        {style => {
            return ();
        }}
    </Motion>);
};

export default Caliper;